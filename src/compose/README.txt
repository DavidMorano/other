COMPOSE

This prorgram is a small "fixer" program that reads an old (existing)
"keyboard" file and prints out the character table prefixed with
the decimal and hexadecial character codes associated with the
character in the table entry (being the first column of the table).

